import React from 'react';
import { StyleSheet, Text, View } from 'react-native';
import MenuButton from '../components/MenuButton';

const Cards = () => {
  return (
    <View style>
      <MenuButton title="Zorunlu Deprem Sİgortası (DASK)" />
      <MenuButton title="AAAAAA KİKİKİKİKİKİ" />
      <MenuButton title="PEZEVENK PEZEVENK PEZEVEEEEENNKK" />
      <MenuButton title="HA BU RİZE DE AMİCAN VEFAD ETMİŞTUR" />
    </View>
  );
};

export default Cards;

const styles = StyleSheet.create({});
